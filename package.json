{
  "name": "itsagency-test-task",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "dependencies": {
    "vite-plugin-handlebars": "^2.0.0"
  },
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && gh-pages -d dist"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "sass": "^1.89.2",
    "vite": "^5.0.0",
    "vite-plugin-handlebars": "^1.6.0"
  }
}
